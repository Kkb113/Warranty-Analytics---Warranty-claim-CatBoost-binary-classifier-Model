# Notebook policy

Notebooks are allowed for exploration, visualization, and temporary
investigation. They must not become the only implementation of ingestion,
validation, feature engineering, training, evaluation, or inference.

Reusable logic must live under src/warranty_analytics_model/. Do not add an
example notebook as part of Phase 1. Notebook checkpoints and generated outputs
are ignored by default.
